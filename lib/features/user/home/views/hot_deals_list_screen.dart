import 'package:flutter/material.dart';

class HotDealsListScreen extends StatelessWidget {
  const HotDealsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hot Deals List Screen'),
      ),
    );
  }
}