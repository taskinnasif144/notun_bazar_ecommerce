import 'package:flutter/material.dart';

class BestSellingListScreen extends StatelessWidget {
  const BestSellingListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Best Selling List Screen'),
      ),
    );
  }
}