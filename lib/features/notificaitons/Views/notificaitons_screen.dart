import 'package:flutter/material.dart';

class NotificaitonsScreen extends StatelessWidget {
  const NotificaitonsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Notificaiton screen"),
      ),
    );
  }
}